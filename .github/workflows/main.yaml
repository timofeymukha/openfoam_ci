name: Compile

# Controls when the action will run. 
on:
  push:
     branches: [master]
  
jobs:
  v2212:
      name: "v2212"
      runs-on: ubuntu-20.04
      container: opencfd/openfoam2112-dev
      steps:
        - name: Checkout
          uses: actions/checkout@v3
          with:
            fetch-depth: 1
        - name: Compile
          run: |
           ls /usr/lib/openfoam/openfoam2212/etc/

